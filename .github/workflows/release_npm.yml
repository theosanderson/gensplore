name: Release Gensplore Component

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  release:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: gensplore-component
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - name: Enable the pinned Yarn version
        run: |
          npm install --global corepack@0.36.0
          corepack enable
          yarn --version
      - name: Verify npm authentication
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          if [ -z "$NODE_AUTH_TOKEN" ]; then
            echo "::error::Set the NPM_TOKEN repository secret to an npm token with publish access to gensplore."
            exit 1
          fi
          npm whoami --registry=https://registry.npmjs.org
      - run: yarn install --immutable
      - run: yarn test
      - name: Set the release version
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: npm version "${RELEASE_TAG#v}" --no-git-tag-version
      - run: yarn build
        env:
          NODE_OPTIONS: --max-old-space-size=4096
      - name: Publish
        run: npm publish --ignore-scripts --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
